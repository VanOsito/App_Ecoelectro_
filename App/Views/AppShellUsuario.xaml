<?xml version="1.0" encoding="utf-8" ?>
<Shell
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:App.Views"
    x:Class="App.Views.AppShellUsuario"
    Title="AppShellUsuario"
    Shell.NavBarHasShadow="False"
    Shell.BackgroundColor="#7F4A9B"
    Shell.TitleColor="White"
    Shell.ForegroundColor="White"
    Shell.DisabledColor="#B57AFF"
    Shell.UnselectedColor="White"
    Shell.TabBarBackgroundColor="#7F4A9B"
    Shell.TabBarForegroundColor="White"
    Shell.TabBarTitleColor="White"
    Shell.TabBarUnselectedColor="#E8D9FF">

    <TabBar>
        <ShellContent
            Title="Inicio"
            Icon="home.png"
            ContentTemplate="{DataTemplate views:InicioPage}" />
        <ShellContent
            Title="Cámara"
            Icon="camara.png"
            ContentTemplate="{DataTemplate views:CameraPage}" />

        <ShellContent
            Title="Perfil"
            Icon="user.png"
            ContentTemplate="{DataTemplate views:PerfilPage}" />
        <ShellContent
            Title="Punto de Reciclaje"
            Icon="mapa.png"
            ContentTemplate="{DataTemplate views:MapaPage}" />
    </TabBar>

</Shell>
